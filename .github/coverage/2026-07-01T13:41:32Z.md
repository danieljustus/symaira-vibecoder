<!-- coverage: timestamp=2026-07-01T13:41:32Z repo=danieljustus/symaira-vibecoder head=d35905191e278d26f710c0f7ee71494b7cd06417 overall=54.3% patch=n/a baseline=54.3% delta=0.0% mutation=n/a assertdensity=2.4 slowtests=2 suite=8.5 -->

# Coverage Review - 2026-07-01

## Summary
- Tooling: Go built-in coverage (`go test ./... -coverprofile` + `go tool cover`)
- Command: `go test ./... -coverprofile=coverage.out -covermode=atomic && go tool cover -func=coverage.out`
- Result: pass
- Overall: lines 54.3% (no branch/function coverage available from stdlib tool)
- Patch coverage: n/a (no Go code diff versus v0.4.0; only `.github/CODEOWNERS`, `.github/FUNDING.yml`, `.goreleaser.yml` changed)
- Baseline: 54.3% → 54.3% (delta 0.0%); no regression
- Mutation score: n/a (not run — no diff to scope, no mutation tool configured)
- Test quality: assert density 2.4 asserts/test, 0 zero-assert test files; AAA no systemic issues; suite 8.5s, 2 slow tests (>1s)
- Gate: warn-only (no hard repo threshold configured; default 80% bar from rule 17 noted but not enforced)

## Metrics
| Area | Lines | Notes |
|---|---:|---|
| `cmd/symvibe` | 35.2% | CLI entrypoints; low coverage expected for main/pair/serve glue |
| `internal/auth` | 94.4% | well-tested middleware |
| `internal/browser` | 0.0% | no test file (platform-specific `Open`) |
| `internal/config` | 61.2% | cycle/resolve well-covered; discover/template gaps |
| `internal/devices` | 78.1% | registry core well-tested |
| `internal/engine` | 52.5% | sensors/bus partially covered; pause/resume untested |
| `internal/runner` | 41.2% | API runner tested; CLI/local_api/opencode runners have gaps |
| `internal/server` | 62.5% | handlers_cycle/pairing have uncovered edit/delete paths |
| `internal/server/tlsutil` | 78.2% | cert generation well-tested |
| `internal/version` | 0.0% | no test file (trivial build-info) |
| `web` | 0.0% | embedded static assets (no Go code to test) |
| **Total** | 54.3% | no hard threshold configured |
| **Patch (changed lines)** | n/a | no Go code changes since v0.4.0 |

## Test quality
- **Mutation:** n/a (not run — no Go diff to scope, no mutation tool configured in repo)
- **Assert density:** 2.4 asserts/test overall (361 assertions across 149 tests); no zero-assert test files detected
- **AAA:** no systemic issues detected; tests follow standard arrange-act-assert structure
- **Performance:** suite 8.5s; slowest: `cmd/symvibe::TestDoctorCommand` 1.19s, `cmd/symvibe::TestDoctorCommandJSON` 1.16s (both shell out to check PATH tools — expected for integration-style doctor checks); 2 tests over 1s threshold

## Findings
No issue-worthy findings. Overall coverage is stable versus the v0.4.0 baseline (delta 0.0%), and the repository does not define a hard coverage gate. The only changes since v0.4.0 are GitHub metadata files (`.github/CODEOWNERS`, `.github/FUNDING.yml`) and a `.goreleaser.yml` edit, none of which affect code coverage.

## Non-issues
- `internal/browser`, `internal/version`, and `web/` are packages with no test files or purely embedded content; their 0% coverage is expected and does not represent actionable gaps.
- The 54.3% overall coverage is below the default 80% bar (rule 17), but this is a warn-only signal — the repo has no configured threshold, and no code changed since the last measurement, so there is no actionable regression or new gap to report.

## Next step
Run `/02-gh-plan --from-coverage .github/coverage/2026-07-01T13:41:32Z.md` to create issues from any findings (none here).
